# Add your Gemini API key here for AI-powered intent parsing
# Without it, the backend uses a keyword-based fallback (still fully functional)
GEMINI_API_KEY=

INITIAL_BUDGET=5000000
MAX_FLOORS=10
MAX_ROOMS_PER_FLOOR=6
